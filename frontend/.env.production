# Frontend Production Configuration
VITE_API_BASE_URL=https://animalineage.ovh/
VITE_APP_ENV=production

# Build configuration (Vite)
# Note: Ces options ne sont plus nécessaires avec Vite
# Vite génère automatiquement des builds optimisés